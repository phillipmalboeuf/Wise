{% if image != blank %}
  <style>
    .{{class}} {
      background-image: url('{{image.src|img_url: 'master'|replace: '//cdn.shopify.com', 'https://wisemens.imgix.net'}}&auto=format,compress&w=1500');
    }

    @media screen and (max-width: 1200px) {
      .{{class}} {
        background-image: url('{{image.src|img_url: 'master'|replace: '//cdn.shopify.com', 'https://wisemens.imgix.net'}}&auto=format,compress&w=1200}');
      }
    }

    @media screen and (max-width: 900px) {
      .{{class}} {
        background-image: url('{{image.src|img_url: 'master'|replace: '//cdn.shopify.com', 'https://wisemens.imgix.net'}}&auto=format,compress&w=900}');
      }
    }

    /*@media screen and (max-width: 600px) {
      .{{class}} {
        background-image: url('{{image.src| img_url: '600x' }}');
      }
    }   */ 
  </style>
  {% endif %}